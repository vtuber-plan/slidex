# Changelog

本项目遵循语义化版本。发布日期以 Git tag 和 GitHub Release 为准。

## 1.7.0-rc.6 - 2026-09-20 — PPTX 兼容性与导出位置

- 修复 ZIP 头字段、中央目录及本地条目偏移；补齐核心属性内容类型、幻灯片版式关系、备注回链，分离备注母版主题以兼容 PowerPoint。
- 修复可编辑 PPTX 背景 XML 层级、默认圆角和文本框额外内边距；图片缺失及页面渲染超时明确报错。
- 桌面导出 PNG 弹出文件夹选择，PDF／PPTX／HTML 弹出保存对话框；取消不执行导出，自定义路径支持结果下载和中文文件名。
- PPTX 统一格式入口，默认可编辑优先；另提供视觉保真（整页图片）模式并解释编辑限制。CLI 的 --editable 行为保持兼容。
- 移除导出浏览器额外字体抗锯齿覆盖，加入独立 ZIP 阅读、Open XML SDK 校验、实际 PowerPoint 打开与渲染比对流程。

## 1.7.0-rc.5 - 2026-09-20 — 桌面菜单与 DSL 工具

- 文件菜单集中偏好设置与导出；语言、浅色／深色、自动保存持久化在当前设备，Electron 原生菜单调用同一设置和导出窗口。
- 工具菜单提供源码、格式化、即时语法诊断与 LLM 图片导出；诊断跟随源码草稿，格式化保留富文本、代码、公式、属性和注释。
- PNG 支持当前页、全部页、范围与离散页码，保留原始页码命名；可附 JSON 清单映射页面 ID 和图片文件。
- CLI 新增 format（标准输出／--write／--check）、validate --json、export --pages 1,3-5 --manifest；所有格式遇文档错误时拒绝导出。

## 1.7.0-rc.4 - 2026-09-20 — 画布导航与工作区收口

- 区分适应画布与手动缩放：100% 使用文档实际尺寸，可输入 10–400% 比例，手动比例不随面板尺寸变化。
- Ctrl/Command+滚轮围绕鼠标缩放，空格拖动或中键平移，普通滚轮平移、Shift+滚轮横向平移；Esc 可取消正在进行的平移。
- 左右面板可折叠并记忆状态；文字编辑时保留设计／图层／动画标签，切换标签会先提交文字，返回文字编辑时自动选中设计页签。
- 导航状态不写入 DSL，不进入文档撤销历史；既有原位文字与嵌套组合坐标共用更新后的视图变换。

## 1.7.0-rc.3 - 2026-09-20 — 稳定画布与品牌更新

- 文字格式工具固定在右侧属性区，不再在画布上方插入工具栏；溢出提示不参与布局，编辑前后画布尺寸和位置保持一致。
- 双击按点击位置定位文字光标；格式、段落、链接与公式继续支持选区编辑。
- 更新折页式 S 标识、应用图标与安装器素材；简化画布背景和顶栏层级。桌面原生菜单默认收起，可按 Alt 访问。

## 1.7.0-rc.2 - 2026-09-20 — 工作区交互修复

- 幻灯片栏、属性栏和备注区域支持拖动调整尺寸、键盘调整及记忆尺寸。
- 桌面应用内“打开本地文件”接入系统文件选择器，移除 Electron 不支持的 prompt；浏览器提供路径输入对话框。
- 原生菜单和应用菜单共用打开流程，切换前提交文字并保存修改；取消或无效文件保持原文档，切换后更新窗口标题。
- 文本沿用画布内直接编辑。旧 `release/win-unpacked` 为 1.6.1，本版解压程序位于 `release/1.7.0-rc.2/win-unpacked`。

## 1.7.0-rc.1 - 2026-09-20 — React Studio 候选版

- 支持嵌套组合内直接编辑、原位文字、图片裁剪、多选属性和表格合并区结构编辑。
- 新增段落排版、链接/行内公式编辑、外观面板及中英文常规界面；英文诊断提供说明和可展开的原始详情。
- 修复组合旋转/翻转坐标、文字输入事务、零坐标序列化和渐变/图片背景保存重载。
- 大文档缩略图按可见范围渲染；拖动仅复制当前页，整文档校验在手势结束后更新。
- 内置公式、图标及其字体随导出内联，项目内字体样式与字体文件可内联到独立 HTML。用户配置的远程字体、图片仍需网络。
- Windows 原生输入法候选窗尚待人工验收，本版本不标记为正式发布。

- 正式编辑器迁移到 React、Tailwind CSS、Radix Themes 与 Zustand，构建使用 Vite。
- ProseMirror 取代正式入口的 execCommand；新增结构化表格/图表面板和资源浏览。
- 新画布支持多选、框选、吸附、旋转、缩放、组合和按手势撤销。
- React Player 与独立 HTML 共用动画内核，补齐 HTML 动画与切换；新增预览网格和同源嵌入 API。
- 新增自动保存、本地 20 版历史、文件版本冲突检测，兼容 Electron 保存/另存为。
- 迁移搜索替换、格式复制、等距分布、右键菜单和系统剪贴板；桌面原生编辑菜单接入文档事务。
- 编辑器与 Viewer 分路由懒加载；原界面保留在 `/legacy` 供兼容回归。
- React 公式与图标资源本地打包，修复深色播放器污染默认表格文字颜色。

## 1.6.1 - 2026-09-19

### DSL 与渲染

- 新增递归 `group`、元素链接、内部页面跳转、`alt`、`locked` 和 `lock-aspect`。
- 自动补齐页面与元素 ID，并加强 chart、table、fill、crop、布尔值、范围和引用校验。
- 修复空 chart、空 deck、CSV 引号、文本 fill、图片填充路径和独立 HTML 路径越界问题。
- 独立 HTML 正确转义标题和页面 ID；明确本地媒体内联与在线公式/图标/字体的能力边界。

### 编辑器与放映

- 新增分组/解组、对齐分布、智能参考线、旋转手柄、元素锁定和等比缩放。
- 新增缩略图拖拽排序、媒体导入、四边裁剪、动画排序/预览和系统剪贴板。
- 新增母版可视化编辑、deck/主题/字体面板、表格行列操作、图表系列/坐标轴编辑。
- 新增搜索替换、自动恢复和磁盘外部修改检测。
- 修复导出旧状态、撤销顺序、内联列表损坏、动画串联、旋转/翻转丢失和演讲者视图问题。

### 质量

- 新增 DSL 与真实浏览器回归测试，覆盖解析、序列化、渲染、编辑、媒体上传与导出前保存。
- 文档同步到当前实现，并补充下一版能力边界。
